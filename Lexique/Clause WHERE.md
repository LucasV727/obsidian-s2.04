#sql #filtre #requete

La clause SQL utilisée pour extraire uniquement les [[Enregistrement|lignes]] qui répondent à un critère précis. Par exemple : `WHERE pays = 'IUT'` pour ne garder que les étudiants de votre établissement.
